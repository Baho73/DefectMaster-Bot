# Развертывание DefectMaster Bot на VPS

## 📋 Данные вашего сервера

```
IP: 5.180.24.215
User: root
Password: U19TlpZ301V4
Location: US (Haedus)
```

---

## 🚀 Быстрый деплой (5 минут)

### Шаг 1: Подключение к серверу

**Windows (PowerShell или CMD):**
```bash
ssh root@5.180.24.215
```

При первом подключении будет вопрос:
```
Are you sure you want to continue connecting (yes/no)?
```
Ввести: **yes**

Затем ввести пароль: **U19TlpZ301V4**

---

### Шаг 2: Установка Docker (на сервере)

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Установка Docker Compose
apt install docker-compose -y

# Проверка установки
docker --version
docker-compose --version
```

---

### Шаг 3: Создание структуры проекта

```bash
# Создание директории
mkdir -p /root/defectmaster-bot
cd /root/defectmaster-bot

# Создание папки для БД
mkdir -p data
```

---

### Шаг 4: Загрузка файлов на сервер

**С вашего компьютера (новое окно PowerShell):**

```bash
# Переход в папку проекта
cd D:\Python\Stroykontrol_mvp_01

# Копирование файлов на сервер
scp -r * root@5.180.24.215:/root/defectmaster-bot/
```

Ввести пароль: **U19TlpZ301V4**

**Альтернатива - через Git (если проект в репозитории):**
```bash
# На сервере
cd /root/defectmaster-bot
git clone <ваш-репозиторий> .
```

---

### Шаг 5: Запуск бота

**На сервере:**

```bash
cd /root/defectmaster-bot

# Запуск через Docker Compose
docker-compose up -d

# Проверка логов
docker-compose logs -f
```

**Ожидаемый результат:**
```
defectmaster-bot | Bot started successfully!
defectmaster-bot | Press Ctrl+C to stop
```

Нажмите `Ctrl+C` для выхода из логов (бот продолжит работать).

---

## ✅ Проверка работы

1. Откройте Telegram
2. Найдите бота: **@Stroy_Control_001_bot**
3. Нажмите `/start`
4. Должно прийти приветствие и создаться Google Таблица

---

## 🔧 Управление ботом на сервере

### Просмотр статуса
```bash
docker-compose ps
```

### Просмотр логов
```bash
# Последние 100 строк
docker-compose logs --tail=100

# В реальном времени
docker-compose logs -f

# Только ошибки
docker-compose logs | grep ERROR
```

### Перезапуск
```bash
docker-compose restart
```

### Остановка
```bash
docker-compose down
```

### Обновление после изменений
```bash
# Остановка
docker-compose down

# Получение изменений (если через Git)
git pull

# Или загрузка файлов через scp

# Пересборка и запуск
docker-compose up -d --build
```

---

## 📁 Важные файлы на сервере

```
/root/defectmaster-bot/
├── .env                    # Конфигурация (токены)
├── service-account.json    # Google credentials
├── docker-compose.yml      # Docker настройки
├── data/
│   └── bot.db             # База данных
└── [остальные файлы проекта]
```

---

## 🗄️ Резервное копирование

### Бэкап базы данных

```bash
# Создание бэкапа
docker cp defectmaster-bot:/app/data/bot.db /root/backups/bot_$(date +%Y%m%d).db

# Автоматический бэкап (добавить в crontab)
crontab -e

# Добавить строку (бэкап каждый день в 3:00)
0 3 * * * docker cp defectmaster-bot:/app/data/bot.db /root/backups/bot_$(date +\%Y\%m\%d).db
```

### Восстановление из бэкапа

```bash
# Остановка бота
docker-compose down

# Копирование бэкапа
cp /root/backups/bot_20251120.db /root/defectmaster-bot/data/bot.db

# Запуск
docker-compose up -d
```

---

## 🔒 Безопасность

### Изменение пароля root

```bash
passwd
```

Введите новый пароль.

### Настройка файрвола (UFW)

```bash
# Установка UFW
apt install ufw -y

# Разрешить SSH
ufw allow 22/tcp

# Включить файрвол
ufw enable

# Проверка статуса
ufw status
```

### Отключение SSH по паролю (опционально, для продакшена)

1. Сгенерировать SSH ключ на вашем компьютере
2. Скопировать на сервер
3. Отключить вход по паролю

---

## 📊 Мониторинг ресурсов

### Использование памяти и CPU

```bash
# Общая информация
docker stats

# Информация о контейнере
docker stats defectmaster-bot

# Использование диска
df -h
```

### Логи системы

```bash
# Системные логи
journalctl -xe

# Логи Docker
journalctl -u docker
```

---

## ⚠️ Устранение проблем

### Бот не запускается

```bash
# Проверить логи
docker-compose logs

# Проверить .env файл
cat .env

# Проверить service-account.json
cat service-account.json | head -n 5
```

### Нет интернета в контейнере

```bash
# Перезапуск Docker
systemctl restart docker

# Перезапуск бота
docker-compose down && docker-compose up -d
```

### Закончилось место на диске

```bash
# Очистка неиспользуемых образов
docker system prune -a

# Проверка размера БД
du -h data/bot.db
```

---

## 🔄 Обновление Docker образа

```bash
cd /root/defectmaster-bot

# Остановка
docker-compose down

# Обновление кода
git pull  # или scp файлы

# Пересборка образа
docker-compose build --no-cache

# Запуск
docker-compose up -d
```

---

## 📝 Полезные команды

```bash
# Вход в контейнер (для отладки)
docker exec -it defectmaster-bot /bin/bash

# Просмотр переменных окружения
docker exec defectmaster-bot env

# Копирование файла из контейнера
docker cp defectmaster-bot:/app/bot.db ./bot.db

# Копирование файла в контейнер
docker cp ./new-file.txt defectmaster-bot:/app/
```

---

## 🎯 Автозапуск при перезагрузке

Docker Compose уже настроен на `restart: always`, поэтому бот автоматически запустится после перезагрузки сервера.

Проверить:
```bash
# Перезагрузка сервера
reboot

# После перезагрузки подключиться и проверить
ssh root@5.180.24.215
docker ps
```

---

## 📞 Быстрая помощь

**Бот не отвечает?**
1. `docker-compose logs` - проверить логи
2. `docker ps` - убедиться что контейнер запущен
3. Проверить токены в `.env`

**Ошибки Google API?**
1. Проверить `service-account.json`
2. Убедиться что APIs включены в Google Cloud Console

**Ошибки AI?**
1. Проверить `GOOGLE_API_KEY` в `.env`
2. Проверить квоты в Google AI Studio

---

## ✅ Чеклист деплоя

- [ ] Подключился к серверу SSH
- [ ] Установил Docker и Docker Compose
- [ ] Создал директорию `/root/defectmaster-bot`
- [ ] Скопировал файлы на сервер
- [ ] Проверил `.env` (все токены на месте)
- [ ] Проверил `service-account.json` (загружен)
- [ ] Запустил `docker-compose up -d`
- [ ] Проверил логи (нет ошибок)
- [ ] Протестировал бота в Telegram
- [ ] Настроил автоматический бэкап

---

**Готово!** Бот работает на VPS 24/7!

IP сервера: **5.180.24.215**
