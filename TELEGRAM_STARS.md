# Telegram Stars - Руководство по использованию

## ⭐ Что такое Telegram Stars?

**Telegram Stars** - встроенная платежная система Telegram для оплаты цифровых товаров и услуг прямо в приложении.

**Преимущества:**
- ✅ Оплата без выхода из Telegram
- ✅ Поддержка всех популярных способов оплаты (карты, Apple Pay, Google Pay и др.)
- ✅ Безопасность платежей на стороне Telegram
- ✅ Автоматическое обновление баланса
- ✅ Не требуется регистрация в банках или платежных системах
- ✅ Быстрая интеграция

## 💰 Текущие пакеты

| Пакет | Фото | Stars | ~Стоимость |
|-------|------|-------|------------|
| Малый | 20 | 80 ⭐ | ~200₽ |
| Средний | 50 | 160 ⭐ | ~400₽ |
| Большой | 100 | 280 ⭐ | ~700₽ |

*Курс: 1 Star ≈ 2.5₽ (зависит от региона)*

## 🔧 Как работает интеграция

### 1. Пользователь выбирает пакет

```
/balance → Нажимает кнопку "⭐ 50 фото - 160 Stars"
```

### 2. Telegram показывает инвойс

Бот отправляет `send_invoice()` с параметрами:
- Название: "DefectMaster - 50 фото"
- Описание: "Пакет 50 анализов фотографий"
- Цена: 160 Stars
- Валюта: XTR (код для Telegram Stars)

### 3. Пользователь оплачивает

Telegram открывает встроенную форму оплаты:
- Выбор способа оплаты (карта, Apple Pay и т.д.)
- Подтверждение платежа
- Списание Stars

### 4. Автоматическое обновление баланса

После успешной оплаты:
- Бот получает `successful_payment` event
- Проверяет payload
- Начисляет кредиты на баланс
- Сохраняет транзакцию в БД
- Отправляет подтверждение пользователю

## 📊 Реализованные компоненты

### bot/services/stars_payment_service.py

```python
class StarsPaymentService:
    # Пакеты с ценами в Stars
    packages = {
        "small": {"credits": 20, "stars": 80},
        "medium": {"credits": 50, "stars": 160},
        "large": {"credits": 100, "stars": 280}
    }

    # Создание invoice параметров
    get_invoice_params(package_key, user_id)

    # Парсинг payload
    parse_invoice_payload(payload)
```

### bot/handlers/payments.py

```python
# 1. Обработка кнопки покупки
@router.callback_query(F.data.startswith("buy_"))
async def handle_buy_callback(...)

# 2. Pre-checkout проверка
@router.pre_checkout_query()
async def handle_pre_checkout(...)

# 3. Успешный платеж
@router.message(F.successful_payment)
async def handle_successful_payment(...)
```

## 🚀 Как пользоваться (для пользователя)

### Шаг 1: Проверка баланса

```
/balance
```

Бот покажет:
```
💰 Твой баланс: 0 фото

⭐ Пакеты (Telegram Stars):
[⭐ 20 фото - 80 Stars]
[⭐ 50 фото - 160 Stars]
[⭐ 100 фото - 280 Stars]
```

### Шаг 2: Выбор пакета

Нажать на кнопку с нужным пакетом

### Шаг 3: Оплата

Telegram откроет форму оплаты:
1. Проверить сумму
2. Выбрать способ оплаты
3. Нажать "Оплатить"

### Шаг 4: Подтверждение

После оплаты бот пришлет:
```
✅ Платеж успешен!

Начислено: 50 фото
💳 Ваш баланс: 50 фото

Спасибо за покупку! Отправляйте фото для анализа.
```

## 💳 Способы оплаты Stars

Пользователи могут купить Stars через:
- Банковские карты (Visa, Mastercard, МИР)
- Apple Pay
- Google Pay
- Другие платежные системы (зависит от региона)

## 🔒 Безопасность

### Проверка payload

Каждый платеж включает уникальный payload:
```python
payload = f"{user_id}:{package_key}"
# Пример: "123456789:medium"
```

При получении платежа:
1. Парсим payload
2. Проверяем user_id
3. Проверяем package_key
4. Только после проверки начисляем кредиты

### Pre-checkout валидация

Перед оплатой выполняется проверка:
```python
@router.pre_checkout_query()
async def handle_pre_checkout(pre_checkout_query):
    # Проверка payload
    # Проверка user_id
    # Ответ ok=True/False
```

## 💰 Комиссия и вывод средств

### Комиссия Telegram

Telegram берет **~30%** от каждой транзакции. Это стандартная комиссия платформ (как App Store, Google Play).

**Пример:**
- Пользователь платит 160 Stars (≈400₽)
- Комиссия Telegram: ~48 Stars (≈120₽)
- Вы получаете: ~112 Stars (≈280₽)

### Вывод средств

Заработанные Stars можно:
1. **Вывести на банковский счет** через Fragment.com
2. **Использовать для покупок** в других Telegram ботах
3. **Обменять на рекламу** в Telegram Ads

**Минимальная сумма вывода:** ~$100 (эквивалент в Stars)

**Процесс вывода:**
1. Перейти на https://fragment.com/
2. Войти через Telegram
3. Выбрать "Withdraw Stars"
4. Указать банковские реквизиты
5. Подтвердить вывод

## 📈 Мониторинг платежей

### В базе данных

Все платежи сохраняются в таблице `payments`:

```sql
SELECT * FROM payments WHERE status = 'completed';
```

Поля:
- `order_id` - Telegram payment charge ID
- `user_id` - ID пользователя
- `amount` - Количество Stars
- `credits` - Начисленные кредиты
- `status` - Статус (pending/completed)
- `created_at` - Дата создания

### Статистика

Для аналитики можно запросить:
```sql
-- Всего платежей
SELECT COUNT(*) FROM payments WHERE status = 'completed';

-- Общая сумма в Stars
SELECT SUM(amount) FROM payments WHERE status = 'completed';

-- Топ пакетов
SELECT credits, COUNT(*) as count
FROM payments
WHERE status = 'completed'
GROUP BY credits
ORDER BY count DESC;
```

## 🧪 Тестирование

### Тестовая оплата

В режиме разработки можно использовать **Telegram Test Environment**:

1. Создать тестового бота через @BotFather в тестовой среде
2. Использовать тестовый токен
3. Проводить тестовые платежи без реальных денег

**Примечание:** В продакшн среде тестовые платежи невозможны - Stars списываются реально.

### Проверка интеграции

После запуска бота:
1. Отправить `/balance`
2. Нажать на любой пакет
3. Проверить, что открывается форма оплаты
4. Проверить все поля в invoice

## ❓ FAQ

**Q: Сколько стоит 1 Star?**
A: Зависит от региона. В России ~2-3₽, в других странах эквивалент в местной валюте.

**Q: Какая комиссия Telegram?**
A: ~30% от суммы платежа (стандарт для платформ).

**Q: Можно ли изменить цены?**
A: Да, отредактируйте `bot/services/stars_payment_service.py`:
```python
self.packages = {
    "small": {"credits": 20, "stars": 100},  # изменить здесь
    ...
}
```

**Q: Как вывести заработанные Stars?**
A: Через https://fragment.com/ (минимум ~$100)

**Q: Нужна ли регистрация ИП/ООО?**
A: Нет, для Telegram Stars не требуется. Для вывода средств может потребоваться в зависимости от суммы.

**Q: Можно ли вернуть деньги пользователю?**
A: Технически нет автоматического refund API. Возврат возможен вручную через поддержку Telegram.

**Q: Работает ли в России?**
A: Да, Telegram Stars доступны в России.

## 🔧 Изменение цен

Если нужно изменить цены или добавить новые пакеты:

**bot/services/stars_payment_service.py:**
```python
self.packages = {
    "small": {
        "credits": 20,
        "stars": 80,  # ← изменить количество Stars
        "title": "20 фото",
        "description": "Пакет 20 анализов фотографий"
    },
    # Добавить новый пакет:
    "mega": {
        "credits": 500,
        "stars": 1200,
        "title": "500 фото",
        "description": "Мега-пакет для профи"
    }
}
```

## 📞 Поддержка

- **Документация Telegram Stars:** https://core.telegram.org/bots/payments
- **Fragment (вывод средств):** https://fragment.com/
- **Telegram Bot API:** https://core.telegram.org/bots/api#payments

## ✅ Чеклист готовности

- [x] Код интеграции написан (`stars_payment_service.py`)
- [x] Обработчики платежей созданы (`payments.py`)
- [x] Pre-checkout проверка реализована
- [x] Автоматическое обновление баланса работает
- [x] Запись в БД настроена
- [ ] Получен Gemini API ключ (для AI)
- [ ] Включены Google APIs
- [ ] Протестирован реальный платеж

---

**Текущий статус:** Интеграция Telegram Stars полностью готова к работе!

После получения Gemini API ключа можно запускать бота.
