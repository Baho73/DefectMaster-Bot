# Настройка Tinkoff Merchant API

## 🧪 Тестовый режим (уже настроен)

В `.env` уже добавлены тестовые ключи Tinkoff:
```
TINKOFF_TERMINAL_KEY=TinkoffBankTest
TINKOFF_SECRET_KEY=TinkoffBankTest
```

**Тестовые данные работают для:**
- Разработки и отладки
- Проверки интеграции
- Демонстрации функционала

**Ограничения тестового режима:**
- Реальные деньги НЕ переводятся
- Платежи можно только симулировать
- Нет реальных уведомлений

---

## 💼 Продакшн режим (для реальных платежей)

### Шаг 1: Регистрация в Tinkoff

1. Перейдите на: https://www.tinkoff.ru/business/internet-acquiring/
2. Нажмите **"Подключить"**
3. Заполните заявку:
   - Данные компании (ИП или ООО)
   - ИНН
   - Контактные данные
4. Дождитесь одобрения (обычно 1-3 рабочих дня)

### Шаг 2: Получение ключей API

После одобрения:

1. Войдите в **Личный кабинет** Tinkoff Business
2. Перейдите в раздел **"Интернет-эквайринг"**
3. Найдите раздел **"API"** или **"Настройки интеграции"**
4. Скопируйте:
   - **Terminal Key** (TerminalKey)
   - **Secret Key** (Password)

### Шаг 3: Обновление конфигурации

Замените в `.env`:

```env
TINKOFF_TERMINAL_KEY=ваш_реальный_terminal_key
TINKOFF_SECRET_KEY=ваш_реальный_secret_key
```

### Шаг 4: Настройка уведомлений (Webhooks)

Для автоматического обновления баланса после оплаты:

1. В личном кабинете Tinkoff найдите **"Уведомления"** или **"Webhooks"**
2. Укажите URL вашего сервера:
   ```
   https://ваш-домен.ru/tinkoff/webhook
   ```
3. Сохраните настройки

**Важно:** Для webhooks нужен:
- Публичный домен с HTTPS
- Открытый порт (обычно 443)
- Обработчик webhook на сервере

---

## 🔧 Как работает интеграция

### Реализованный функционал

В `bot/services/payment_service.py` уже реализовано:

1. **Инициализация платежа** (`init_payment`)
   - Создание заказа
   - Генерация ссылки на оплату
   - Расчет токена безопасности

2. **Проверка статуса** (`check_payment_status`)
   - Получение статуса платежа по ID
   - Обновление данных в БД

3. **Валидация уведомлений** (`verify_notification`)
   - Проверка подписи от Tinkoff
   - Защита от подделки уведомлений

### Как пользователь покупает кредиты

1. Пользователь нажимает `/balance`
2. Выбирает пакет (20, 50 или 100 фото)
3. Бот создает платеж через Tinkoff API
4. Пользователь получает ссылку на оплату
5. После оплаты:
   - **В тестовом режиме:** нужно вручную обновить баланс
   - **В продакшене:** webhook автоматически обновит баланс

---

## 💰 Тарифы и комиссии Tinkoff

**Комиссия Tinkoff:**
- От 2.4% до 3.5% с каждой транзакции (зависит от оборота)
- Нет абонентской платы
- Подключение бесплатно

**Текущие пакеты в боте:**
- 20 фото - 199₽ (комиссия ~6₽)
- 50 фото - 399₽ (комиссия ~12₽)
- 100 фото - 699₽ (комиссия ~21₽)

**Вы получаете:** Сумма платежа минус комиссия Tinkoff

---

## 🧪 Тестирование платежей

### В тестовом режиме

```python
# Уже реализовано в bot/handlers/payments.py
# Пользователь нажимает кнопку покупки
# Бот генерирует тестовую ссылку на оплату
```

**Тестовые карты Tinkoff:**
- Успешная оплата: `4300000000000777`
- Отклоненная оплата: `4300000000000034`
- CVV: любой (например, 123)
- Срок действия: любой будущий (например, 12/25)

### Проверка интеграции

После запуска бота:

1. Отправьте боту `/balance`
2. Нажмите на пакет
3. Проверьте, что генерируется ссылка на оплату
4. Перейдите по ссылке
5. Используйте тестовую карту `4300000000000777`

---

## 🔒 Безопасность

### Генерация токена

Каждый запрос к Tinkoff подписывается токеном:

```python
# Реализовано в payment_service.py
token = sha256(TerminalKey + Amount + OrderId + Password)
```

Это защищает от:
- Подделки запросов
- Изменения сумм платежей
- Man-in-the-middle атак

### Валидация webhook

Уведомления от Tinkoff проверяются:
```python
# Реализовано в payment_service.py
verify_notification(notification_data)
```

---

## 🚀 Реализация webhook (для продакшена)

Для автоматического обновления баланса создайте файл `webhook_server.py`:

```python
from aiohttp import web
from bot.services.payment_service import payment_service
from bot.database.models import db

async def handle_tinkoff_webhook(request):
    """Handle Tinkoff payment notification"""
    data = await request.json()

    # Verify signature
    if not payment_service.verify_notification(data):
        return web.Response(status=400, text="Invalid signature")

    # Check status
    if data.get('Status') == 'CONFIRMED':
        order_id = data.get('OrderId')

        # Get payment from DB
        payment = await db.get_payment(order_id)
        if payment:
            # Update balance
            await db.update_balance(payment['user_id'], payment['credits'])
            await db.update_payment_status(order_id, 'completed')

            return web.Response(text="OK")

    return web.Response(text="OK")

app = web.Application()
app.router.add_post('/tinkoff/webhook', handle_tinkoff_webhook)

if __name__ == '__main__':
    web.run_app(app, port=8080)
```

---

## 📊 Мониторинг платежей

### В базе данных

Все платежи сохраняются в таблице `payments`:

```sql
SELECT * FROM payments WHERE status = 'pending';
SELECT * FROM payments WHERE status = 'completed';
```

### В личном кабинете Tinkoff

- Все транзакции видны в реальном времени
- История платежей
- Аналитика и отчеты
- Выгрузка в Excel

---

## ❓ FAQ

**Q: Можно ли протестировать без регистрации в Tinkoff?**
A: Да! Используйте тестовые ключи, которые уже в `.env`

**Q: Сколько стоит подключение?**
A: Подключение бесплатно. Комиссия только с транзакций (2.4-3.5%)

**Q: Нужен ли ИП/ООО?**
A: Да, для продакшена нужна регистрация как ИП или ООО

**Q: Когда деньги поступают на счет?**
A: Обычно T+1 (на следующий рабочий день)

**Q: Можно ли использовать другую платежную систему?**
A: Да, можно адаптировать код под ЮKassa, Stripe и др.

---

## 📞 Поддержка Tinkoff

- **Сайт:** https://www.tinkoff.ru/business/
- **Телефон:** 8 800 755-25-50
- **Email:** business@tinkoff.ru
- **Документация API:** https://developer.tinkoff.ru/

---

## ✅ Чеклист интеграции

- [x] Код интеграции написан
- [x] Тестовые ключи добавлены
- [ ] Получены реальные ключи (для продакшена)
- [ ] Настроены webhooks (для автоматического обновления баланса)
- [ ] Протестированы платежи с тестовой картой
- [ ] Проверена работа в продакшене

---

**Текущий статус:** Интеграция готова к тестированию с тестовыми ключами!

Для запуска в продакшене нужно получить реальные ключи от Tinkoff.
