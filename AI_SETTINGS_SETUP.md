# Настройка AI через Google Docs

## Зачем это нужно?

Вынос настроек AI (моделей и промптов) в Google Docs позволяет:
- ✅ Быстро менять промпты без перезапуска бота
- ✅ Экспериментировать с разными моделями Gemini
- ✅ Тестировать новые инструкции для AI
- ✅ Настраивать бота через удобный интерфейс Google Docs

## Шаг 1: Создайте Google Doc

1. Откройте [Google Docs](https://docs.google.com)
2. Создайте новый документ: **Создать** → **Google Документы**
3. Назовите его (например: "DefectMaster AI Settings")

## Шаг 2: Заполните документ настройками

Скопируйте и вставьте следующий шаблон в ваш документ:

```
---
RELEVANCE_MODEL: gemini-2.5-flash
ANALYSIS_MODEL: gemini-2.5-pro
---
RELEVANCE_PROMPT:
РОЛЬ:
Ты — строгий и опытный инженер строительного контроля (Технадзор) в РФ.

ЗАДАЧА:
Проверь, является ли фото строительным. Если это кот, еда, селфи или явный мусор — верни "is_relevant": false и шутливый комментарий.

ФОРМАТ ОТВЕТА (JSON):
{
  "is_relevant": true/false,
  "joke": "Текст шутки если не релевантно",
  "items": [],
  "expert_summary": null
}

ВАЖНО: Отвечай ТОЛЬКО валидным JSON. Никакого дополнительного текста.
---
ANALYSIS_PROMPT:
РОЛЬ:
Ты — строгий и опытный инженер строительного контроля (Технадзор) в РФ. Твоя цель — найти нарушения, оценить их критичность и сослаться на нормы.

ЗАДАЧА:
1. Используй предоставленный пользователем контекст (Объект/Место).
2. Найди дефекты. Для каждого дефекта определи:
   - Наименование.
   - Точное местонахождение на фото.
   - Критичность (Критический / Значительный / Малозначительный).
   - Вероятная причина.
   - Нарушенная норма РФ (СП, ГОСТ, СНиП, Приказ Минтруда). Обязательно укажи номер пункта.
   - Рекомендация по устранению (императив: "Сделать", "Устранить").
3. Сформируй краткое экспертное заключение по фото.

ФОРМАТ ОТВЕТА (JSON):
{
  "is_relevant": true,
  "joke": null,
  "items": [
    {
      "defect": "Название дефекта",
      "location": "Где именно на фото",
      "criticality": "Критический",
      "cause": "Причина",
      "norm": "СП 70.13330 п. 5.17.4",
      "recommendation": "Текст рекомендации"
    }
  ],
  "expert_summary": "Текст заключения (2-3 предложения)."
}

ВАЖНО: Отвечай ТОЛЬКО валидным JSON. Никакого дополнительного текста.
---
```

### Структура документа:

1. **Разделитель `---`** - разделяет секции
2. **RELEVANCE_MODEL** - модель для проверки релевантности (быстрая)
3. **ANALYSIS_MODEL** - модель для детального анализа (точная)
4. **RELEVANCE_PROMPT** - промпт для проверки релевантности фото
5. **ANALYSIS_PROMPT** - промпт для детального анализа дефектов

### Доступные модели Gemini:

- `gemini-2.5-flash` - быстрая, дешевая (рекомендуется для RELEVANCE_MODEL)
- `gemini-2.5-flash-lite` - еще быстрее и дешевле
- `gemini-2.5-pro` - точная, дорогая (рекомендуется для ANALYSIS_MODEL)
- `gemini-3-pro-preview` - самая новая и точная (экспериментальная)

## Шаг 3: Поделитесь документом с сервисным аккаунтом

1. В Google Docs нажмите **Настройки доступа** (кнопка "Share" справа вверху)
2. В поле добавления пользователей вставьте email сервисного аккаунта:
   ```
   audiostend@audiostend.iam.gserviceaccount.com
   ```
3. Выберите роль **Читатель** (Viewer) - этого достаточно
4. Снимите галочку "Уведомить пользователей"
5. Нажмите **Отправить**

## Шаг 4: Скопируйте ID документа

1. Посмотрите на URL открытого документа:
   ```
   https://docs.google.com/document/d/1a2B3c4D5e6F7g8H9i0J/edit
   ```
2. ID документа - это часть между `/d/` и `/edit`:
   ```
   1a2B3c4D5e6F7g8H9i0J
   ```
3. Скопируйте этот ID

## Шаг 5: Добавьте ID в .env

1. Откройте файл `.env` в корне проекта
2. Найдите строку:
   ```env
   GOOGLE_SETTINGS_DOC_ID=
   ```
3. Вставьте ID документа:
   ```env
   GOOGLE_SETTINGS_DOC_ID=1a2B3c4D5e6F7g8H9i0J
   ```
4. Сохраните файл

## Шаг 6: Перезапустите бота

```bash
python main.py
```

## Как это работает?

1. **При каждой загрузке фото** бот читает настройки из Google Docs
2. Если документ не найден или есть ошибка - используются дефолтные настройки
3. Вы можете менять модели и промпты прямо в Google Docs
4. Изменения применяются **сразу** - перезапуск бота не нужен!

## Примеры использования

### Пример 1: Смена модели на более быструю

Измените в документе:
```
ANALYSIS_MODEL: gemini-2.5-flash
```

Это ускорит анализ и снизит стоимость, но может уменьшить точность.

### Пример 2: Добавление специфичных инструкций

Добавьте в `ANALYSIS_PROMPT`:
```
ДОПОЛНИТЕЛЬНО:
- Особое внимание обращай на электробезопасность
- Проверяй соблюдение СП 256.1325800.2016 (Электроустановки жилых и общественных зданий)
```

### Пример 3: Изменение стиля ответа

Добавьте в промпт:
```
СТИЛЬ:
- Используй технический язык
- Будь кратким и конкретным
- Критичность оценивай строго
```

## Отключение динамических настроек

Если хотите вернуться к дефолтным настройкам:

1. Откройте `.env`
2. Очистите или закомментируйте строку:
   ```env
   # GOOGLE_SETTINGS_DOC_ID=
   ```
3. Перезапустите бота

## Кэширование

По умолчанию настройки кэшируются после первого чтения:
- ✅ Быстрее работает (не читает документ каждый раз)
- ❌ Изменения применяются с задержкой

Чтобы отключить кэш и читать документ каждый раз, используйте API:
```python
from bot.services.settings_service import settings_service
settings_service.disable_cache()
```

## Логирование

Все операции с настройками логируются:
- Чтение документа
- Парсинг настроек
- Используемые модели
- Ошибки при чтении

Проверяйте логи бота для отладки.

## Устранение проблем

### Ошибка: "Failed to read settings from Google Docs"

**Решение:**
1. Проверьте, что документ существует
2. Убедитесь, что дали доступ сервисному аккаунту
3. Проверьте правильность ID документа
4. Убедитесь, что включен Google Docs API в Google Cloud Console

### Бот использует дефолтные настройки, хотя документ настроен

**Решение:**
1. Проверьте формат документа (должны быть разделители `---`)
2. Проверьте логи бота - там будет информация об ошибках парсинга
3. Убедитесь, что `GOOGLE_SETTINGS_DOC_ID` заполнен в `.env`

### Модель не найдена

**Ошибка:** `Model gemini-xxx not found`

**Решение:**
1. Проверьте название модели в документе
2. Используйте только официальные модели Gemini
3. Убедитесь, что модель доступна в вашем регионе

## Пример полного workflow

1. Создал Google Doc с настройками
2. Поделился с `audiostend@audiostend.iam.gserviceaccount.com`
3. Скопировал ID документа
4. Добавил в `.env`: `GOOGLE_SETTINGS_DOC_ID=1a2B3c4D5e6F7g8H9i0J`
5. Перезапустил бота
6. Отправил тестовое фото
7. Проверил логи - бот читает настройки из документа
8. Изменил промпт в документе
9. Отправил еще одно фото - новый промпт сразу применился!

## Дополнительно

- Документ можно редактировать прямо во время работы бота
- Можно создать несколько документов для разных конфигураций
- Можно использовать Google Docs API для автоматизации настроек
- Можно интегрировать версионирование через Google Docs history

---

**Готово!** Теперь вы можете настраивать AI через удобный интерфейс Google Docs 🎉
